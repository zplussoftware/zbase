<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/static/css/styles.css">
</head>
<body>
    <%- include('../partials/header') %>

    <section class="hero-section">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-6">
                    <h1 class="display-4 fw-bold">ZBase Authentication & Authorization System</h1>
                    <p class="lead my-4">
                        A powerful, secure, and flexible authentication and authorization system built with NestJS and PostgreSQL.
                        Perfect for managing user accounts, roles, and permissions in modern web applications.
                    </p>
                    <div class="d-grid gap-2 d-md-flex justify-content-md-start">
                        <% if (!user || !user.isAuthenticated) { %>
                            <a href="/login" class="btn btn-primary btn-lg px-4 me-md-2">Login</a>
                            <a href="/register" class="btn btn-outline-secondary btn-lg px-4">Register</a>
                        <% } else { %>
                            <% if (user.isAdmin) { %>
                                <a href="/admin" class="btn btn-primary btn-lg px-4 me-md-2">Admin Dashboard</a>
                            <% } %>
                            <button id="logout-button" class="btn btn-outline-secondary btn-lg px-4">Logout</button>
                        <% } %>
                    </div>
                </div>                <div class="col-lg-6">
                    <img src="/images/auth-illustration.svg" alt="Authentication Illustration" class="img-fluid" onerror="if (!this.hasAttribute('data-error-handled')) { this.setAttribute('data-error-handled', 'true'); this.src='/images/default-avatar.png'; }">
                </div>
            </div>
        </div>
    </section>

    <section class="py-5 bg-light">
        <div class="container">
            <div class="row mb-4">
                <div class="col-lg-8 mx-auto text-center">
                    <h2 class="fw-bold">Key Features</h2>
                    <p class="lead text-muted">Explore what makes our authentication system stand out</p>
                </div>
            </div>
            <div class="row g-4">
                <div class="col-md-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <h5 class="card-title">JWT Authentication</h5>
                            <p class="card-text">Secure, token-based authentication using JSON Web Tokens (JWT) for stateless API requests.</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <h5 class="card-title">Role-Based Access Control</h5>
                            <p class="card-text">Implement fine-grained access control with customizable user roles and permissions.</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <h5 class="card-title">User Management</h5>
                            <p class="card-text">Comprehensive user management interface for administrators to manage accounts and permissions.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="py-5">
        <div class="container">
            <div class="row mb-4">
                <div class="col-lg-8 mx-auto text-center">
                    <h2 class="fw-bold">Demo Accounts</h2>
                    <p class="lead text-muted">Try our system with these demo accounts</p>
                </div>
            </div>
            <div class="row">
                <div class="col-lg-8 mx-auto">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>Role</th>
                                    <th>Email</th>
                                    <th>Password</th>
                                    <th>Description</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td><span class="badge bg-danger">Admin</span></td>
                                    <td>admin@example.com</td>
                                    <td>admin123</td>
                                    <td>Full access to all features including user management</td>
                                </tr>
                                <tr>
                                    <td><span class="badge bg-warning text-dark">Manager</span></td>
                                    <td>manager@example.com</td>
                                    <td>manager123</td>
                                    <td>Access to admin panel with limited privileges</td>
                                </tr>
                                <tr>
                                    <td><span class="badge bg-primary">User</span></td>
                                    <td>user@example.com</td>
                                    <td>user123</td>
                                    <td>Regular user with standard privileges</td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <%- include('../partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <% if (user && user.isAuthenticated) { %>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const logoutButton = document.getElementById('logout-button');
            if (logoutButton) {
                logoutButton.addEventListener('click', function() {
                    fetch('/api/auth/logout', {
                        method: 'POST',
                        credentials: 'include',
                        headers: {
                            'Content-Type': 'application/json'
                        }
                    })
                    .then(response => response.json())
                    .then(data => {
                        window.location.href = '/';
                    })
                    .catch(error => {
                        console.error('Logout error:', error);
                        window.location.href = '/';
                    });
                });
            }
        });
    </script>
    <% } %>
</body>
</html>